
/** 
 * RevenueCat Service - Stubbed for Full Access Version
 */

export const isRevenueCatInitialized = () => true;

export const initRevenueCat = async (userId: string): Promise<boolean> => {
  return true;
};

export const checkEntitlement = async (): Promise<boolean> => {
  return true;
};

export const getOfferings = async () => {
  return null;
};

export const purchasePackage = async (pkg: any) => {
  return true;
};

export const restorePurchases = async () => {
  return true;
};
